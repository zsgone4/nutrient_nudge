diff --git a/src/WorkletsModule/NativeWorklets.native.ts b/src/WorkletsModule/NativeWorklets.native.ts
--- a/src/WorkletsModule/NativeWorklets.native.ts
+++ b/src/WorkletsModule/NativeWorklets.native.ts
@@ -26,7 +26,8 @@ class NativeWorklets implements IWorkletsModule {
       global.__workletsModuleProxy === undefined &&
       globalThis.__RUNTIME_KIND === RuntimeKind.ReactNative
     ) {
-      WorkletsTurboModule?.installTurboModule(bundleModeEnabled);
+      // Pass as string for compatibility with older native binary builds
+      (WorkletsTurboModule as any)?.installTurboModule?.(bundleModeEnabled ? 'true' : 'false');
       if (__DEV__ && bundleModeEnabled) {
         console.log(
           '[Worklets] Bundle mode initialization: Downloaded the bundle for Worklet Runtimes.'
